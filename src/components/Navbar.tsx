import React from 'react';

const styledNavbar = {
  backgroundColor: 'grey',
};

const Navbar = () => {
  return (
    <nav style={styledNavbar}>
      <h4>navbar</h4>
    </nav>
  );
};

export default Navbar;
